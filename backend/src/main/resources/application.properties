# ===================================
# PERFIL ACTIVO
# ===================================
# Cambia entre 'dev' (H2) y 'prod' (MySQL)
spring.profiles.active=prod

# Las configuraciones específicas están en:
# - application-dev.properties (H2 en memoria)
# - application-prod.properties (MySQL persistente)
